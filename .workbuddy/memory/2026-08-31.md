# 2026-08-31

## anki-tv-vocab 每日同步到 GitHub（自动化 1785848000656）
- 运行 `GIT_SYNC_HELPER=store bash git-sync.sh`（dangerouslyDisableSandbox 走宿主机网络）
- 改动检测有改动 → 本地提交成功：`5db53b5` `chore: sync 2026-08-31`（1 file changed, 9 insertions+，自动化 memory.md 触发）
- 推送失败（exit 128）：`Failed to connect to github.com port 443 after 21073 ms: Could not connect to server` —— 环境到 GitHub 的 HTTPS 出站持续不可达（与 08-29/08-30 三连一致），非脚本/凭据问题
- 凭据：store 助手读取缓存 token 正常，无交互式登录框
- 处理：仅记录错误，不重试阻断；下次运行会自动重新 push（本地 commit 已落地）
